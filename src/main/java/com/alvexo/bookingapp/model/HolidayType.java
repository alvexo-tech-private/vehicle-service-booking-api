package com.alvexo.bookingapp.model;

public enum HolidayType {
    /** Workshop fully closed — no bookings accepted. */
    HOLIDAY,
    /** Workshop open but not accepting new bookings for the day. */
    PAUSE
}
